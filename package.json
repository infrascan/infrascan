{
  "packageManager": "npm",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^1.9.9"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build"
  }
}
