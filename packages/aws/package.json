{
  "name": "@infrascan/aws",
  "version": "0.5.6",
  "description": "A convenience package to register all AWS Scanners with an instance of the Infrascan SDK",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "require": "./dist/index.js",
      "import": "./dist/index.mjs"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist ; tsup --config internal/tsconfig/tsup.json",
    "prepublish": "rm -rf dist ; tsup --config internal/tsconfig/tsup.json",
    "test": "echo \"Error: no test specified\"",
    "lint": "tsc --noEmit ; prettier src/**/*.ts -c ; eslint .",
    "lint:fix": "prettier src/**/*.ts -w ; eslint . --fix",
    "clean": "rm -rf dist node_modules"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/infrascan/infrascan.git"
  },
  "keywords": [
    "AWS",
    "Cloud Infrastructure",
    "Infrastructure Diagram",
    "Scanner"
  ],
  "author": "Liam Farrelly <liam@l3f7.dev> (https://whoisli.am/)",
  "license": "MPL-2.0",
  "bugs": {
    "url": "https://github.com/infrascan/infrascan/issues"
  },
  "homepage": "https://github.com/infrascan/infrascan#readme",
  "devDependencies": {
    "@infrascan/sdk": "workspace:^0.6.2",
    "@infrascan/tsconfig": "workspace:*"
  },
  "dependencies": {
    "@infrascan/aws-api-gateway-scanner": "workspace:^0.5.0",
    "@infrascan/aws-autoscaling-scanner": "workspace:^0.3.1",
    "@infrascan/aws-cloudfront-scanner": "workspace:^0.5.0",
    "@infrascan/aws-cloudwatch-logs-scanner": "workspace:^0.5.0",
    "@infrascan/aws-dynamodb-scanner": "workspace:^0.5.0",
    "@infrascan/aws-ec2-scanner": "workspace:^0.7.0",
    "@infrascan/aws-ecs-scanner": "workspace:^0.6.0",
    "@infrascan/aws-elastic-load-balancing-scanner": "workspace:^0.5.0",
    "@infrascan/aws-kinesis-scanner": "workspace:^0.4.0",
    "@infrascan/aws-lambda-scanner": "workspace:^0.5.0",
    "@infrascan/aws-rds-scanner": "workspace:^0.5.0",
    "@infrascan/aws-route53-scanner": "workspace:^0.6.0",
    "@infrascan/aws-s3-scanner": "workspace:^0.5.0",
    "@infrascan/aws-sns-scanner": "workspace:^0.5.0",
    "@infrascan/aws-sqs-scanner": "workspace:^0.5.0",
    "@infrascan/aws-step-function-scanner": "workspace:^0.4.0"
  }
}
