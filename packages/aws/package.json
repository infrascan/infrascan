{
  "name": "@infrascan/aws",
  "version": "0.5.7",
  "description": "A convenience package to register all AWS Scanners with an instance of the Infrascan SDK",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "require": "./dist/index.js",
      "import": "./dist/index.mjs"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist ; tsup --config internal/tsconfig/tsup.json",
    "prepublish": "rm -rf dist ; tsup --config internal/tsconfig/tsup.json",
    "test": "echo \"Error: no test specified\"",
    "lint": "tsc --noEmit ; prettier src/**/*.ts -c ; eslint .",
    "lint:fix": "prettier src/**/*.ts -w ; eslint . --fix",
    "clean": "rm -rf dist node_modules"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/infrascan/infrascan.git"
  },
  "keywords": [
    "AWS",
    "Cloud Infrastructure",
    "Infrastructure Diagram",
    "Scanner"
  ],
  "author": "Liam Farrelly <liam@l3f7.dev> (https://whoisli.am/)",
  "license": "MPL-2.0",
  "bugs": {
    "url": "https://github.com/infrascan/infrascan/issues"
  },
  "homepage": "https://github.com/infrascan/infrascan#readme",
  "devDependencies": {
    "@infrascan/sdk": "workspace:^",
    "@infrascan/tsconfig": "workspace:*"
  },
  "dependencies": {
    "@infrascan/aws-api-gateway-scanner": "workspace:^",
    "@infrascan/aws-autoscaling-scanner": "workspace:^",
    "@infrascan/aws-cloudfront-scanner": "workspace:^",
    "@infrascan/aws-cloudwatch-logs-scanner": "workspace:^",
    "@infrascan/aws-dynamodb-scanner": "workspace:^",
    "@infrascan/aws-ec2-scanner": "workspace:^",
    "@infrascan/aws-ecs-scanner": "workspace:^",
    "@infrascan/aws-elastic-load-balancing-scanner": "workspace:^",
    "@infrascan/aws-kinesis-scanner": "workspace:^",
    "@infrascan/aws-lambda-scanner": "workspace:^",
    "@infrascan/aws-rds-scanner": "workspace:^",
    "@infrascan/aws-route53-scanner": "workspace:^",
    "@infrascan/aws-s3-scanner": "workspace:^",
    "@infrascan/aws-sns-scanner": "workspace:^",
    "@infrascan/aws-sqs-scanner": "workspace:^",
    "@infrascan/aws-step-function-scanner": "workspace:^"
  }
}
